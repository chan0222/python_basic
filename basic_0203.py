hello = "안녕하세요"
print(hello[0:2])
print(hello)

## []기호를 이용해 문자열의 특징 위치에 있는 문자를 참조하는 것을 인덱싱
## [:] 기호를 이용해 문자열의 일부를 추출 하는 것을 슬라이싱이라고 함. 

## 문자열 연결 연산자 + // 문자열은 무조건 문자열끼리 + 기호를 사용해서 연결해야하고, 숫자라 하더라도 문자열과 함께 + 연산하려면 큰 따옴표를 붙여 문자열로 인식 시켜야만 에러 없이 결과를 얻을 수있음
## 숫자를 더할때는 반드시 숫자와 숫자 사이에 + 기호를 사용해서 연산

## 문자열 반복 연산자 *